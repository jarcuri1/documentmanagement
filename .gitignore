__pycache__/
*.pyc

# Automation browser profiles — contain live session cookies (SmartMLS,
# TenantTracks). NEVER commit these.
chrome-profile/
chrome-profile-tenanttracks/

# Reference PDFs generated locally for overlay alignment (regenerable).
single_family_lease.pdf
multi_family_lease.pdf

# LibreOffice isolated conversion profiles (created next to converted PDFs).
.lo-*/
.lo-.*
